version: 2.1

jobs:
  build-and-test:
    docker:
      - image: cimg/node:20.5.0
    steps:
      - checkout
      - run: npm ci
      - run: npm run lint
      - run: npm test

workflows:
  build_and_test:
    jobs:
      - build-and-test
